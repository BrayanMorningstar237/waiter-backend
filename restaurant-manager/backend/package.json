{
  "name": "backend",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "seed": "node seed.js",
    "seed:reset": "node seed.js",
    "db:info": "node -e \"require('dotenv').config(); const mongoose = require('mongoose'); mongoose.connect(process.env.MONGODB_URI).then(async () => { const Restaurant = require('./models/Restaurant'); const User = require('./models/User'); const restaurants = await Restaurant.countDocuments(); const users = await User.countDocuments(); console.log('📊 Database Info:'); console.log('Restaurants:', restaurants); console.log('Users:', users); if (restaurants > 0) { const restList = await Restaurant.find().select('name'); console.log('Restaurant names:', restList.map(r => r.name)); } process.exit(0); })\""
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "cloudinary": "^1.41.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.1.0",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.19.1",
    "multer": "^2.0.2",
    "multer-storage-cloudinary": "^4.0.0"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
